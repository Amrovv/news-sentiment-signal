# Findings

Running results log — kept from the merge & sanity session onward, so the
write-up becomes assembly, not archaeology (see docs/project_overview.md §8).

One entry per finding: date · what · evidence/figure · caveat.

Calibration: 53–57% out-of-sample directional accuracy is a good result.
70%+ almost certainly means leakage — investigate, don't celebrate.

---

## Sanity session
_(pending — first merge of articles.parquet + labels_features.parquet)_
